---

import Footer from '../components/Footer.astro';
import Layout from '../layouts/Layout.astro';

<meta charset="UTF-8">

  <!-- Metaetiquetas y datos estructurados -->
  <head>
    <title>Altura Creativa | Soluciones de Marketing Digital y Diseño Web</title>
    <meta name="description" content="Agencia de marketing digital y diseño web para hacer crecer tu negocio. Ofrecemos soluciones personalizadas para aumentar tu presencia online y tus ventas.">
    <meta name="keywords" content="marketing digital, diseño web, agencia de marketing, desarrollo web, soluciones digitales">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="https://tu-dominio.com">
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": "Altura creativa | Marketing Digital y Diseño Web",
      "description": "Especialistas en marketing digital y diseño web. Impulsamos tu presencia digital con estrategias personalizadas.",
      "url": "https://tu-dominio.com",
      "publisher": {
        "@type": "Organization",
        "name": "Altura creativa",
        "logo": {
          "@type": "ImageObject",
          "url": "https://tu-dominio.com/logo.png"
        }
      }
    }
    </script>
    <style>
      body {
        background: linear-gradient(180deg, #121212, #000);
        color: white;
        overflow-x: hidden;
        position: relative;
        font-family: 'Inter', sans-serif;
      }
      .particles {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
      }
      .content-container {
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
        backdrop-filter: blur(10px);
      }
      h2 {
        color: #ff2600;
      }
      p {
        color: #cccccc;
        line-height: 1.6;
      }
    </style>
  </head>
  <Layout>
  
  <!-- Contenido de la página -->
  <main class="relative flex justify-center items-center min-h-screen">
    <div class="particles">
      <canvas id="particleCanvas"></canvas>
    </div>
    <div class="content-container">
      <h2 class="text-3xl font-bold">Política de Privacidad</h2>
      <p>
        En Altura Creativa SL, nos tomamos muy en serio la privacidad y seguridad de tus datos personales. Cumplimos con el Reglamento General de Protección de Datos (RGPD) para garantizar que tu información se maneje de forma segura y conforme a la ley.
      </p>
      <h3 class="text-xl mt-4">1. Datos que recopilamos</h3>
      <p>Solo recopilamos datos necesarios como nombre, correo electrónico y número de teléfono, los cuales nos proporcionas voluntariamente.</p>
      <h3 class="text-xl mt-4">2. Uso de los datos</h3>
      <p>Usamos tus datos para mejorar nuestros servicios y personalizar tu experiencia.</p>
      <h3 class="text-xl mt-4">3. Protección de tus datos</h3>
      <p>Implementamos medidas avanzadas de seguridad como cifrado y firewalls.</p>
      <h3 class="text-xl mt-4">4. Tus derechos</h3>
      <p>Puedes acceder, modificar o eliminar tus datos en cualquier momento.</p>
      <h3 class="text-xl mt-4">5. Contacto</h3>
      <p>Email: privacidad@alturacreativa.com | Teléfono: +34 123 456 789</p>
    </div>
  </main>
  <footer class="py-8 text-center text-gray-400">
    <Footer />
  </footer>
</Layout>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const canvas = document.getElementById("particleCanvas");
    const ctx = canvas.getContext("2d");
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    const particlesArray = [];
    class Particle {
      constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * canvas.height;
        this.size = Math.random() * 2 + 1;
        this.speedX = (Math.random() - 0.5) * 0.5;
        this.speedY = (Math.random() - 0.5) * 0.5;
      }
      update() {
        this.x += this.speedX;
        this.y += this.speedY;
        if (this.x >= canvas.width || this.x <= 0) this.speedX *= -1;
        if (this.y >= canvas.height || this.y <= 0) this.speedY *= -1;
      }
      draw() {
        ctx.fillStyle = "rgba(255, 255, 255, 0.3)";
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.closePath();
        ctx.fill();
      }
    }

    function initParticles() {
      for (let i = 0; i < 80; i++) {
        particlesArray.push(new Particle());
      }
    }

    function animateParticles() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particlesArray.forEach(particle => {
        particle.update();
        particle.draw();
      });
      requestAnimationFrame(animateParticles);
    }

    initParticles();
    animateParticles();
  });
</script>