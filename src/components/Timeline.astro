<section class="relative bg-transparent flex flex-col items-center justify-center min-h-screen">
  <div class="absolute inset-0 -z-10 opacity-40 bg-[radial-gradient(60rem_60rem_at_top,theme(colors.indigo.900),transparent)]"></div>

  <h2 class="text-3xl font-semibold text-center text-white mb-12 fade-in">Lo que dicen nuestros clientes</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-8 max-w-5xl w-full">
    
    {[
      { name: "Ana Rodríguez", role: "Directora de eventos", quote: "Contratamos el servicio de sonido e iluminación para nuestra gala anual, y fue simplemente espectacular. Profesionalismo y calidad en cada detalle." },
      { name: "Carlos Fernández", role: "Organizador de festivales", quote: "Nuestro festival necesitaba una producción impecable, y esta empresa superó nuestras expectativas. Puntuales, organizados y con un equipo increíble." },
      { name: "María López", role: "Gerente de marketing", quote: "Gracias a su equipo de videografía, nuestro evento corporativo quedó registrado con una calidad impresionante. Sin duda, volveremos a contar con ellos." },
      { name: "Javier Morales", role: "Músico y productor", quote: "No solo montaron un escenario increíble para nuestro concierto, sino que la acústica fue perfecta. Su experiencia en sonido es inigualable." },
    ].map((testimonial, index) => (
      <div class="testimonial-card opacity-0 translate-y-10 transition-all duration-1000">
        <blockquote class="text-base sm:text-lg font-medium text-gray-300">{testimonial.quote}</blockquote>
        <figcaption class="mt-4 text-white font-semibold">{testimonial.name}</figcaption>
        <div class="text-gray-400 text-sm">{testimonial.role}</div>
      </div>
    ))}

  </div>
</section>

<style>
@keyframes fade-in {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.testimonial-card {
  background: rgba(20, 20, 20, 0.85);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 24px;
  text-align: center;
  transition: all 0.3s ease-in-out;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
  opacity: 0;
  transform: translateY(50px); /* Desplazamiento inicial fuera de la vista */
}

.testimonial-card.opacity-100 {
  opacity: 1;
  transform: translateY(0); /* Regresar a la posición original */
}

.testimonial-card:hover {
  border-color: rgba(99, 102, 241, 0.5);
  box-shadow: 0 4px 25px rgba(99, 102, 241, 0.2);
  transform: scale(1.02);
  background: rgba(20, 20, 20, 0.9);
}
</style>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const elements = document.querySelectorAll(".testimonial-card");

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("opacity-100");
      }
    });
  }, { threshold: 0.2 });

  elements.forEach((el) => observer.observe(el));
});
</script>
