<section class="relative bg-transparent py-24 sm:py-32">
  <div class="mx-auto max-w-7xl px-6 lg:px-8 text-center">
    <div class="relative bg-gray-950 px-10 py-16 shadow-2xl ring-1 ring-gray-800 rounded-3xl opacity-0 translate-y-6 transition-all duration-1000 fade-in">
      
      <!-- Contenido de texto -->
      <h2 class="text-4xl font-semibold text-white sm:text-5xl leading-tight">
        Haz que tu evento sea inolvidable.
      </h2>
      <p class="mt-6 text-lg text-gray-400 leading-relaxed">
        Deja que nuestros expertos se encarguen de cada detalle, desde sonido hasta videografía. ¡Transforma tu evento con nosotros!
      </p>
      
      <!-- Botones -->
      <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-6">
        <!-- Botón de llamada -->
        <a href="tel:+34641437667" class="cta-button">
          📞 ¡Llama ahora!
        </a>
        <!-- Más información -->
        <a href="#mas-info" class="text-sm font-semibold text-gray-300 hover:text-indigo-400 hover:underline transition">
          Más información →
        </a>
      </div>

      <!-- Glow en el fondo -->
      <div class="absolute inset-0 bg-gradient-to-r from-transparent via-indigo-900/30 to-transparent rounded-3xl blur-2xl opacity-50 transition-transform duration-500 scale-100 hover:scale-105"></div>
    </div>
  </div>
</section>

<!-- Estilos y animaciones -->
<style>
@keyframes fade-in {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

.cta-button {
  display: inline-block;
  padding: 12px 30px;
  font-size: 1.125rem;
  font-weight: 700;
  color: white;
  background: linear-gradient(to right, #6366f1, #8b5cf6);
  border-radius: 8px;
  box-shadow: 0px 4px 15px rgba(99, 102, 241, 0.4);
  transition: all 0.3s ease-in-out;
  cursor: pointer;
}

.cta-button:hover {
  background: linear-gradient(to right, #4f46e5, #6d28d9);
  box-shadow: 0px 6px 20px rgba(99, 102, 241, 0.6);
  transform: scale(1.05);
}

.cta-button:focus {
  outline: none;
  box-shadow: 0px 0px 8px rgba(99, 102, 241, 0.7);
}
</style>

<script>
document.addEventListener("DOMContentLoaded", () => {
  // Seleccionar el elemento que debe ser animado
  const fadeInElement = document.querySelector(".fade-in");

  // Configurar IntersectionObserver para que active la animación cuando entre en la vista
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        // Cuando el elemento entra en la vista, añadir las clases necesarias
        entry.target.classList.remove("opacity-0", "translate-y-6");
        entry.target.classList.add("opacity-100", "translate-y-0");
      }
    });
  }, { threshold: 0.3 });

  if (fadeInElement) {
    observer.observe(fadeInElement);
  }
});
</script>
