---
const faqItems = [
  {
    question: "¿Qué tipo de eventos gestionamos?",
    answer: "Nos especializamos en la producción y gestión de eventos corporativos, bodas, conciertos, congresos y cualquier tipo de evento en vivo. Nos encargamos de todos los detalles técnicos y logísticos para garantizar una experiencia impecable."
  },
  {
    question: "¿Qué incluye nuestro servicio de sonido en vivo?",
    answer: "Proporcionamos equipos de sonido de última generación, incluyendo sistemas de PA, micrófonos inalámbricos, monitores in-ear y más."
  },
  // Agrega más preguntas aquí...
];
---

<section class="py-16 "> <!-- Fondo negro aplicado -->
  <div class="container mx-auto px-6 max-w-4xl">
    <h2 class="text-4xl font-bold text-center text-gray-100 mb-12">Preguntas Frecuentes</h2>
    <div class="space-y-6">
      {faqItems.map((item, index) => (
        <details class="group rounded-2xl bg-gray-900 border border-gray-800 shadow-lg overflow-hidden transition-all duration-300 open:border-indigo-500 open:shadow-2xl">
          <summary class="flex justify-between items-center cursor-pointer px-6 py-4 text-gray-200 font-medium text-lg group-open:bg-gray-800 group-open:text-white transition-all duration-300 hover:bg-gray-700 rounded-xl">
            {item.question}
            <span class="transition-transform duration-300 group-open:rotate-180">
              <svg class="w-5 h-5" fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </span>
          </summary>
          <p class="px-6 pb-4 pt-2 text-gray-400 text-base opacity-0 translate-y-2 transition-all duration-500 group-open:opacity-100 group-open:translate-y-0">
            {item.answer}
          </p>
        </details>
      ))}
    </div>
  </div>
</section>

<style>
  section {
    background-color: black; /* Fondo negro profundo */
  }

  details {
    background: rgba(15, 15, 15, 0.9);
    backdrop-filter: blur(50px);
    border-radius: 50px;
    padding: 2rem;
    transition: all 0.4s ease-in-out;
  }

  details summary {
    font-size: 1.125rem;
    font-weight: 600;
    padding: 0.75rem;
    background: rgba(15, 15, 15, 0.9);
    border-radius: 12px;
    transition: background-color 0.3s ease-in-out, transform 0.2s;
  }

  details summary:hover {
    background-color: rgba(15, 15, 15, 0.9);
    transform: scale(1.02);
  }

  details[open] {
    border-color: rgba(96, 98, 229, 0.8);
    box-shadow: 0px 5px 16px rgba(99, 102, 241, 0.2);
  }

  details p {
    font-size: 1rem;
    color: #cbd5e1;
  }
</style>
